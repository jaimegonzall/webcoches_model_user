<h1>formulario nuevo usuario</h1>

{{#if error}}
    <h2>{{error}}</h2>
{{/if}}



<form action="/newUser" method="post" id="formNewUser">
    <fieldset>
        {{>atoms/legend texto="Datos personales"}}
        {{>atoms/label labelFor="name" text="Nombre:"}}
        {{>atoms/input type="text" inputName="name" inputPlaceholder="Nombre"}}
        {{>atoms/label labelFor="surname" text="Apellidos:"}}
        {{>atoms/input type="text" inputName="surname" inputPlaceholder="Apellidos"}}
    </fieldset>
    <fieldset>
        {{>atoms/legend texto="Datos login"}}
        {{>atoms/label labelFor="userName" text="Nombre de usuario:"}}
        {{>atoms/input type="text" inputName="userName" inputPlaceholder="Nombre de usuario"}}
        {{>atoms/label labelFor="password" text="Contraseña:"}}
        {{>atoms/input type="text" inputName="password" inputPlaceholder="Contraseña"}}
        {{>atoms/label labelFor="avatar" text="Avatar:"}}
        {{>atoms/input type="file" inputName="avatar"}}
    </fieldset>
    <fieldset>
        {{>atoms/legend texto="Datos contacto"}}
        {{>atoms/label labelFor="email" text="Correo electrónico"}}
        {{>atoms/input type="email" inputName="email" inputPlaceholder="Correo electrónico"}}
    </fieldset>
    {{>atoms/button btnType="submit" btnValue="Crear usuario"}}  
</form>

{{#if user}}
    <ul>
        <li>{{this.name}}</li>
        <li>{{this.surname}}</li>
        <li>{{this.avatar}}</li>
    </ul>
{{/if}}